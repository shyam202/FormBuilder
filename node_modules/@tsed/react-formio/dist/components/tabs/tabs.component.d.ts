import { PropsWithChildren } from "react";
export declare function ButtonTab({ icon, back, onClick, isActive, children }: PropsWithChildren<any>): JSX.Element;
export interface TabsItemProps extends Record<string, any> {
    label?: string;
    icon?: string;
}
export interface TabsProps extends Record<string, any> {
    current?: TabsItemProps;
    items?: TabsItemProps[];
    style?: any;
    className?: string;
    onClick?: (item: TabsItemProps) => void;
    i18n?: (f: string) => string;
}
export declare function Tabs({ style, current, items, children, className, onClick, i18n }: PropsWithChildren<TabsProps>): JSX.Element;
